#include <bits/stdc++.h>
using std::cin;
using std::cout;
using std::endl;

int main(){
	
	int n, q;
	cin >> n >> q;
	long a[n]
	for(int i=0; i < n; ++i) cin >> a[n];
	
	while(q--){
		long k; int x;
		long temp=0;
		for(int i=0; i<k; ++i){
			for(int i=n; i>n-x+1;i--){
				a[i]=a[i]
			}
		}
	}
	
	return 0;
}
