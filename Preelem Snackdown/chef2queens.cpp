#include <bits/stdc++.h>
using std::cin;
using std::cout;
using std::endl;

int main(){
	short t;
	cin >> t;
	while(t--){
		short x, y,n,m;
		cin >> n>> m>> x>> y;
		int count=0;
		for(short i=1; i<=n;++i){
			for(short j=1; j<=m; ++j){
				for(short k=1; k<=n;++k){
					for(short l=1; l<=m;++l){
						
					}
				}
			}
		}
	}
	return 0;
}
